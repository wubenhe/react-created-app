import { combineReducers } from 'redux'


const init = (state = false, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
  
export default combineReducers({init});
